; Overrides Sail 99-sail.ini (loaded after). ZIP masivo Tiendanube ~500MB.
[PHP]
upload_max_filesize = 512M
post_max_size = 512M
; PHP default 20 silently drops extra files on carga individual (imagenes[]).
max_file_uploads = 200
