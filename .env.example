# Variables de entorno — Passkeys / WebAuthn

Copiar a `.env` del entorno. Dejar `WEBAUTHN_ENABLED=false` hasta validar login por contraseña tras el despliegue.

```env
WEBAUTHN_ENABLED=false
WEBAUTHN_RP_ID=gelianv.neobash.site
WEBAUTHN_RP_NAME=GELIA-NV
WEBAUTHN_ORIGINS=https://gelianv.neobash.site,https://localhost,http://localhost
WEBAUTHN_ANDROID_PACKAGE=mx.neobash.gelianv
# SHA-256 del certificado de firma de la APK (con dos puntos), uno o varios separados por coma.
WEBAUTHN_ANDROID_SHA256_CERT_FINGERPRINTS=
# Origen WebAuthn de Android Credential Manager (base64url del certificado). Varios separados por coma.
# Ejemplo: android:apk-key-hash:kffL-daBUxvHpY-4M8yhTavt5QnFEI2LsexohxrGPYU
WEBAUTHN_ANDROID_APK_KEY_HASHES=
```

- `WEBAUTHN_RP_ID` debe coincidir con el host de producción (sin esquema ni puerto).
- `WEBAUTHN_ORIGINS` lista orígenes HTTPS adicionales (coma). Incluir el origen de la app web y, en desarrollo, `http://localhost`.
- Para la APK móvil: publicar `/.well-known/assetlinks.json` (ruta Laravel) con `WEBAUTHN_ANDROID_SHA256_CERT_FINGERPRINTS` y añadir `WEBAUTHN_ANDROID_APK_KEY_HASHES` para validar assertions nativas.
- Tras activar el flag, ejecutar la migración `webauthn_credentials` si aún no está aplicada.

# Cloudflare R2 — publicidad y medios (carga directa desde el navegador)

```env
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
R2_ENDPOINT=
R2_REGION=auto
R2_URL=
MEDIOS_DRIVER=r2
```

Las credenciales no se envían al frontend. CORS del bucket debe permitir PUT/GET/HEAD y exponer `ETag` desde el origen de GELIA-NV.
